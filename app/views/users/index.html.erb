<%# ユーザー一覧（ユーザー目線） %>
<h2>
  ユーザー一覧
  <i class="fas fa-users"></i>
</h2>
 
<% @users.each do |user| %>
  <% if user.is_withdrawal_flag == true %>
    <%= render "users/info" , user: user %>
  <% end %>
<% end %>