<h2>ジャンル一覧</h2>
<%= render 'admin/genres/form', genre: @new_genre %>

<% @genres.each do |genre| %>
  <table>
    <thead>
      <tr>
        <td>ジャンル名</td>
        <td></td>
        <td></td>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= genre.name %></td>
        <td><%= link_to 'ジャンル編集', edit_admin_genre_path(genre.id) %></td>
      </tr>
    </tbody>
  </table>
<% end %>
