<%# ユーザー 投稿一覧ページ %>
<h2>
  投稿一覧
  <i class="fas fa-mail-bulk"></i>
  <i class="fas fa-mail-bulk"></i>
  <i class="fas fa-mail-bulk"></i>
  全 "<%= @all_posts.count %>" 件
</h2>

<div class="jscroll post-list">
  <% @posts.each do |post| %>
    <%= render 'posts/list', post: post %>
  <% end %>
  <div class="hidden">
    <%= paginate @posts, class: "paginate" %>
  </div>
  <div class="page-load-status">
    <p class="infinite-scroll-request">
    </p>
  </div>
</div>
