<%# コメントのAjax化 %>
$("#comment_area_<%= @post.id %>").html("<%= j(render(partial: 'comments/comment', locals: {comments: @post.comments, post: @post, comment: @new_comment})) %>");
$("#comment_body").val("");
$("#file").val("");
<%# コメントエラーメッセージ  %>
$("#error_explanation").html("<%= j(render(partial: 'comments/error', locals: {comment: @new_comment})) %>");

